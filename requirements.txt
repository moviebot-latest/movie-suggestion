# Pinned versions — the old bot had python-telegram-bot unpinned, which
# risked a breaking major-version upgrade silently breaking the deploy.
python-telegram-bot[webhooks]==21.10
asyncpg==0.30.0
redis==5.2.1
aiohttp==3.11.11
requests
